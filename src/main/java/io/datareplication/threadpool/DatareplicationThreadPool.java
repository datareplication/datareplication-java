package io.datareplication.threadpool;

public class DatareplicationThreadPool {
    // TODO: ??? no idea how to handle this if we want to hide the internal async impl and thus exact type of thread pool,
    //       but specifically need a way to get the unwrapped threadpool into the internal classes without exposing it
}
